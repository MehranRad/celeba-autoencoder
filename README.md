# celeba-autoencoder
A PyTorch implementation of a convolutional autoencoder that learns to compress and reconstruct face images from the CelebA dataset, with training/validation loss tracking and reconstruction visualization.
